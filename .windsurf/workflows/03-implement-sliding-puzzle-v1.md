# /implement-sliding-puzzle-v1

Goal: Implement a solvable sliding puzzle (numbers) with:
- board state
- move validation
- shuffle (solvable)
- win detection
- undo (optional in v1)

Rules:
- Logic in `lib/games/sliding_puzzle/logic/`
- UI in `lib/games/sliding_puzzle/ui/`
- Shared widgets in `lib/ui/` only

Verification:
- flutter analyze
- flutter test
- quick manual run (chrome)

Commit message: "Sliding puzzle v1"
